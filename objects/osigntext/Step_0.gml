letters += spd;
text_current = string_copy(signText, 1, floor(letters));

draw_set_font(fTextSmaller);
if (h == 0) h = string_height(signText);
w = string_width(text_current);

if (letters >= length) && keyboard_check_pressed(ord("E") || !place_meeting(oSignText.x,oSignText.y+3,oPlayer))
{
	instance_destroy();
	if (instance_exists(oSign)) oSign.TextCreated = false;
	if (instance_exists(oSign2)) oSign2.TextCreated = false;
}


